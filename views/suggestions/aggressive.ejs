<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<section class="page-content">
	<div class="passive-aggressive-ul"><h1>Passive Aggressive Suggestions:</h1></div>
	<ul>
    <% suggestions.forEach(suggestion => { %>
      <div class="suggestion-list">
			<li id="list">
				<% if (!suggestion.passive) {  %>
					<a href="/suggestions/<%= suggestion._id %>"><%= suggestion.name %> This suggestion was made <%= suggestion.passive ? 'lovingly. 🥺' : 'aggressively. 😤' %>
					</a>
				</li>
			</div>
		<% }}) %>
	</ul>
</section>

<%- include('../partials/footer') %>
